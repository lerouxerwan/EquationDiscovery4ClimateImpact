import numpy as np

from emulator.climate_impact_emulator.climate_impact_emulator import ClimateImpactEmulator


def generate_plots(emulator: ClimateImpactEmulator, split_name_to_x_and_y: dict[str, tuple[np.ndarray, np.ndarray]],
                   show: bool):
    pass
